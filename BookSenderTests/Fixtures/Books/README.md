# Book Fixtures

Deterministic EPUB 2, EPUB 3, malformed container/package/mimetype/content, PDF,
and permitted-repair fixtures are generated by `FixtureFactory` in the native
test target. Source mappings and expected outcomes are recorded in
`fixture-manifest.json`.
